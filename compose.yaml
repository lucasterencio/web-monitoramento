services:
  nginx: 
    image: nginx:1.32.2  
    container_name: proxy
    ports:
      - 80:80
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - vitao
      - interno


networks:
  vintao:
    external: true

  interno:
    external :false
